package de.jabc.cinco.meta.core.ge.style.model.errorhandling;

public enum ECincoError {
		MAX_CARDINALITY, MAX_IN, MAX_OUT, INVALID_SOURCE, INVALID_TARGET, INVALID_CONTAINER, INVALID_CLONE_TARGET, OK
}
